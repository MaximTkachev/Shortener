application:
  host: localhost:8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/shortener
    username: shortener
    password: shortener
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
